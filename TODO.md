## План: загрузка пользовательского списка датчиков и рабочий набор

### Сервер / API
- [x] Добавить `GET /api/v2/job/sensors`: вернуть текущий рабочий список id (+ `default`/`count`).
- [x] Добавить `POST /api/v2/job/sensors`: принять массив id, валидировать по словарю, сохранить рабочий набор; 0 валидных → 400.
- [x] Range/Start/Play: используют рабочий список; по умолчанию рабочий список = весь словарь.
- [x] При восстановлении/рестарте сервера: рабочий список и словарь поднимаются (дефолт = полный) и отдаются в UI.

### Клиент (UI)
- [x] При загрузке/восстановлении WS/REST: заново `GET /api/v2/sensors` и `GET /api/v2/job/sensors`.
- [x] Кнопка «Загрузить» на вкладке «Управление» и модальный диалог:
  - [x] Режим «Из файла»: textarea/вставка; парсинг строк (id или name), резолв через словарь, сводка принятых/пропущенных, 0 валидных — блокер.
  - [x] Режим «Из существующих»: таблица (как «Датчики») с фильтром, чекбоксами и галкой «все/ничего», кнопки Применить/Отмена, «Сбросить на все».
  - [x] Применение: POST `/api/v2/job/sensors`; валид > 0 — обновить рабочий список и лейбл «Рабочий список датчиков: …»; частично — предупреждение; 0 — блокер.
  - [x] Отмена: не менять текущий набор.
- [x] Блокировать Play/Range/Start, если рабочий список пуст.
- [x] Сброс на «все существующие» — установить рабочий список = все id словаря.

### Тесты
- [x] Go: покрыть `GET/POST /api/v2/job/sensors` (валид/частично/0 → 400, дефолт “все”).
- [x] Playwright: сценарии загрузки из текста, выбор из таблицы, сброс на все, восстановление списка после reload/реконнекта; Play/Range блокируются при пустом наборе.

### Документация
- [x] Обновить DOCS/details/README: описать эндпоинты `/api/v2/job/sensors`, рабочий список и UI кнопку «Загрузить».
